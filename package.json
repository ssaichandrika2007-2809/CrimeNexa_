{
  "name": "crimegraph-mvp",
  "version": "1.0.0",
  "private": true,
  "description": "CrimeGraph — AI-powered criminal network analysis prototype (SIH 2026, NCRB Women Safety Division)",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently -n backend,frontend -c blue,magenta \"npm run dev --prefix backend\" \"npm start --prefix frontend\"",
    "start:backend": "npm start --prefix backend",
    "start:frontend": "npm start --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
